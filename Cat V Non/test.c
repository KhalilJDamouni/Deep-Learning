#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>
#include <math.h>

typedef struct Node
{
    int values[64][64][3];
    int class;
} Node;

int main(void)
{
    /*double** test = malloc(sizeof(double*) * 100);

    int size = 0;
    while(test[size] != NULL)
        ++size;

    printf("%i\n", size);
    printf*/

    double sum = log(0) - log(0);
    printf("%f\n", sum);
}
